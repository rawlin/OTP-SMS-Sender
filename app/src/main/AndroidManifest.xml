<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.otp">

    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.OtpSmsSender">
        <activity android:name=".ui.messageSendingScreen.MessageSendingActivity" />
        <activity android:name=".ui.detailsScreen.DetailsActivity" />
        <activity
            android:name=".ui.mainScreen.MainActivity"
            android:label="@string/app_name"
            android:theme="@style/Theme.OtpSmsSender.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>